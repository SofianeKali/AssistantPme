# =============================================================================
# CONFIGURATION IZYINBOX - ENVIRONNEMENT LOCAL
# =============================================================================
# Copiez ce fichier en .env et remplissez les valeurs appropriées
# ATTENTION: Ne commitez JAMAIS le fichier .env dans Git !
# =============================================================================

# -----------------------------------------------------------------------------
# BASE DE DONNÉES (OBLIGATOIRE)
# -----------------------------------------------------------------------------
# Format PostgreSQL: postgresql://username:password@host:port/database
# Exemple local: postgresql://postgres:password@localhost:5432/izyinbox
# Exemple Neon: postgresql://user:pass@ep-xxx.region.aws.neon.tech/izyinbox
DATABASE_URL=postgresql://postgres:password@localhost:5432/izyinbox

# -----------------------------------------------------------------------------
# SÉCURITÉ (OBLIGATOIRE)
# -----------------------------------------------------------------------------
# Générez des secrets aléatoires avec :
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=changez_moi_secret_aleatoire_tres_long_min_32_caracteres
ENCRYPTION_KEY=changez_moi_autre_cle_secrete_pour_chiffrement_32_chars

# -----------------------------------------------------------------------------
# OPENAI (OBLIGATOIRE pour fonctionnalités IA)
# -----------------------------------------------------------------------------
# Obtenez votre clé sur : https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-proj-xxxxxxxxxxxxx

# -----------------------------------------------------------------------------
# STRIPE (OBLIGATOIRE pour abonnements)
# -----------------------------------------------------------------------------
# Clés de test depuis : https://dashboard.stripe.com/test/apikeys
STRIPE_SECRET_KEY=sk_test_xxxxxxxxxxxxx
VITE_STRIPE_PUBLIC_KEY=pk_test_xxxxxxxxxxxxx

# Pour le webhook (en développement local avec Stripe CLI) :
# 1. Installez Stripe CLI : https://stripe.com/docs/stripe-cli
# 2. Exécutez : stripe listen --forward-to localhost:5000/api/stripe-webhook
# 3. Copiez le webhook signing secret ici :
STRIPE_WEBHOOK_SECRET=whsec_xxxxxxxxxxxxx

# Clés de test pour environnement de test (optionnel)
TESTING_STRIPE_SECRET_KEY=sk_test_xxxxxxxxxxxxx
TESTING_VITE_STRIPE_PUBLIC_KEY=pk_test_xxxxxxxxxxxxx

# -----------------------------------------------------------------------------
# RESEND (OBLIGATOIRE pour envoi d'emails)
# -----------------------------------------------------------------------------
# Obtenez votre clé sur : https://resend.com/api-keys
RESEND_API_KEY=re_xxxxxxxxxxxxx

# -----------------------------------------------------------------------------
# ENVIRONNEMENT
# -----------------------------------------------------------------------------
NODE_ENV=development
PORT=5000

# Adresse d'écoute du serveur (optionnel)
# Par défaut : localhost en local, 0.0.0.0 sur Replit
# Utilisez localhost ou 127.0.0.1 en local pour éviter les problèmes Windows
HOST=localhost

# -----------------------------------------------------------------------------
# AUTHENTIFICATION REPLIT (OPTIONNEL - Uniquement sur Replit.com)
# -----------------------------------------------------------------------------
# ⚠️ EN ENVIRONNEMENT LOCAL, NE PAS DÉFINIR CES VARIABLES !
# Ces variables ne sont disponibles QUE sur la plateforme Replit.com
# En local, l'application utilisera UNIQUEMENT l'authentification Email/Password
# qui fonctionne parfaitement sans ces variables.
#
# Ces variables sont automatiquement fournies par Replit quand vous déployez :
# ISSUER_URL=https://replit.com/oidc
# REPL_ID=(fourni automatiquement)
# REPLIT_DOMAINS=(fourni automatiquement)

# -----------------------------------------------------------------------------
# CONNECTEURS REPLIT (OPTIONNEL)
# -----------------------------------------------------------------------------
# Utilisé uniquement si vous déployez sur Replit avec des intégrations natives
# REPLIT_CONNECTORS_HOSTNAME=
# REPL_IDENTITY=
# WEB_REPL_RENEWAL=

# -----------------------------------------------------------------------------
# STOCKAGE CLOUD (Configuré via l'interface utilisateur)
# -----------------------------------------------------------------------------
# Google Drive et OneDrive se configurent via OAuth dans l'application
# Aucune variable d'environnement requise ici
